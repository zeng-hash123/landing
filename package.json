{
  "name": "landing-monorepo",
  "version": 1.0,
  "scripts": {
    "build:astro": "cd astro-landing && npm install && npm run build",
    "build:frontend": "cd frontend && npm install && npm run build",
    "build": "npm run build:astro && npm run build:frontend"
  },
  "dependencies": {
    "next": "^14.2.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
